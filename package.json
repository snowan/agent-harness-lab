{
  "name": "agent-harness-lab",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "engines": {
    "node": ">=22.13.0"
  },
  "scripts": {
    "dev": "vite",
    "preview": "vite preview",
    "build": "tsc --noEmit && vite build",
    "typecheck": "tsc --noEmit --pretty false",
    "test": "vitest run",
    "test:unit": "vitest run tests/unit",
    "test:contract": "vitest run tests/contract",
    "test:e2e": "npm run build && playwright test",
    "test:all": "npm run typecheck && npm run test:unit && npm run test:contract && npm run test:e2e",
    "proof:pr1": "npm run build && node scripts/capture-proof.mjs pr-01",
    "proof:pr2": "npm run build && node scripts/capture-proof.mjs pr-02",
    "proof:pr3": "npm run build && node scripts/capture-proof.mjs pr-03",
    "proof:pr4": "npm run build && node scripts/capture-proof.mjs pr-04",
    "proof:pr5": "npm run build && node scripts/capture-proof.mjs pr-05",
    "proof:pr6": "npm run build && node scripts/capture-proof.mjs pr-06"
  },
  "dependencies": {
    "react": "^19.1.1",
    "react-dom": "^19.1.1"
  },
  "devDependencies": {
    "@cloudflare/vite-plugin": "1.54.2",
    "@openai/sites-vite-plugin": "0.2.0",
    "@axe-core/playwright": "^4.13.0",
    "@playwright/test": "^1.55.0",
    "@types/node": "^24.3.0",
    "@types/react": "^19.1.10",
    "@types/react-dom": "^19.1.7",
    "@vitejs/plugin-react": "6.1.1",
    "typescript": "^5.9.2",
    "vite": "8.2.2",
    "vitest": "4.1.11",
    "wrangler": "4.127.1"
  }
}
